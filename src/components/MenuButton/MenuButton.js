import './MenuButton.css';

const MenuButton = () => {
    return (
        <div className="menu-button">
            <div className="icon">&#8964;</div>
        </div>
    );
};

export default MenuButton;